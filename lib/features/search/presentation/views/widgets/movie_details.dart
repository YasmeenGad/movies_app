import 'package:flutter/material.dart';

class MovieDetails extends StatelessWidget {
  const MovieDetails({this.id, this.mediaType});
  final id;
  final mediaType;

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
